1. If running locally, must download and extract class folders (i.e. cloud_train, cloud_test) from "archive.zip" and implement file/folder path accordingly.
2. If running via Google Collab, must upload "archive" folder with dataset to Google Drive and use "Mount Drive" to reference folder path.
